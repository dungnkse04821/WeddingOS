# M7 Wedding Lifecycle & Cleanup

Status: M7 overall COMPLETE. M7.1, M7.2A, M7.2B1, M7.2B2, and M7.3 are COMPLETE.

Batch 13 adds owner-only `api_v1.archive_wedding`, plus hidden `internal.begin_wedding_delete` and `internal.finalize_wedding_delete` capabilities. The latter remain non-executable by `authenticated`; pgTAP invokes them as `trusted_function_owner` while preserving `request.jwt.claims`, proving that `auth.uid()` remains the business actor for OWNER/collaborator authorization.

Archive converges at ARCHIVED without a receipt or child deletion. Begin-delete transitions ACTIVE or ARCHIVED to DELETING and preserves membership for recovery. Finalize requires DELETING, deletes trusted-operation receipts explicitly, and relies on the existing Wedding FK cascade graph for child records. Tests prove representative event/group cascade, auth user preservation, unrelated Wedding preservation, hidden capability denial, and lifecycle read-only gating.

Verification: clean reset applied Batch 13; full pgTAP suite PASS, 13 files / 438 assertions. M7.2 Storage Edge orchestration and M7.3 Flutter lifecycle UX remain out of this slice.

M7.2A Trusted Actor Bridge is COMPLETE after Batch 14 verification: service-only `edge_api` begin/finalize bridges accept only the Edge-derived verified actor and DB revalidates active OWNER membership. No client role can execute the bridge or hidden overloads.

M7.2B1 Edge Route Foundation is COMPLETE: `POST /functions/v1/wedding-delete` verifies the bearer with Supabase Auth, derives the actor from `/auth/v1/user`, calls only the service-only begin bridge, and returns guarded `202 DELETING`. Storage cleanup and finalization are explicitly deferred to M7.2B2. The repository-standard Deno test path was restored with official Deno 2.9.5; `supabase/functions/wedding-delete/index_test.ts` passed 3/3 tests. Final DB regression reset applied Batch 14 and pgTAP passed 13 files / 438 assertions / 0 failures.

M7.2B2 Storage Cleanup Engine is COMPLETE. Batch 15 makes an absent Wedding return `DELETED` only through the existing service-only begin bridge, so an authenticated retry receives generic terminal success while a present Wedding still independently requires an ACTIVE OWNER. The route uses the local Supabase Storage HTTP API: `POST /storage/v1/object/list/wedding_media` with `prefix`, `limit`, and `offset`, then `DELETE /storage/v1/object/wedding_media` with bounded `prefixes`. It recursively traverses directory entries, lists 100 entries per page, deletes 100 object paths per batch, relists from Storage, and finalizes only after a fresh empty result. Storage list/delete failures return bounded `DELETE_RETRY_REQUIRED` and leave the Wedding DELETING; retry relists from provider state.

Real local provider verification used disposable local fixtures only: trusted server list/delete returned 200; an ACTIVE OWNER's direct Storage DELETE returned 200 but the object remained readable (the runtime's no-op response semantics), proving no organizer DELETE policy; anon direct private read was denied; the trusted cleanup removed Wedding A cover and nested fixture objects; a fresh list returned empty; and Wedding B's object remained. Real pagination was not forced beyond the local fixture size, so the 100-entry pagination and 100-path batching branch is covered by deterministic Edge tests. Final Edge regression passed 3 files / 13 tests / 0 failures; final DB regression reset applied Batch 15 and pgTAP passed 13 files / 438 assertions / 0 failures. At M7.2B2 slice closure, Flutter lifecycle UX remained deferred to M7.3.

M7.3 Flutter Lifecycle UX is COMPLETE. `WeddingLifecycleService` calls `api_v1.archive_wedding` through the existing RPC path and invokes only the canonical `wedding-delete` Edge function with `wedding_id`; actor, role, bucket, prefix, and object paths are never sent. OWNER-only lifecycle controls provide a data-retaining archive confirmation and a separate irreversible delete flow. Typed deletion confirmation trims whitespace, NFC-normalizes with `unorm_dart`, compares case-insensitively, and preserves Vietnamese diacritic distinctions; the transient input is never persisted.

`HomeScreen` derives OWNER status from the current authenticated member row. ARCHIVED Weddings remain selected/readable with an explicit read-only badge, mutation cards hidden, no unarchive action, and Wedding switching preserved. DELETING Weddings replace the editable dashboard with retry/switch recovery UI. Terminal deletion clears the persisted current Wedding and navigates to the selector; a physically absent selected Wedding is also cleared during reload. Backend/provider errors are mapped only to unauthorized, retry-required, or generic UX messages. Flutter verification passed 31 tests; analyzer passed with 0 errors / 0 warnings / 208 info diagnostics. DB smoke regression passed 13 files / 438 assertions. A pre-existing missing parenthesis and closing State brace in `cover_media_screen.dart` was repaired to restore the analyzer baseline without changing M6 behavior. At M7.3 slice closure, integrated regression and handoff remained deferred to M7.4.

## M7.4 Integrated Closure

The final clean reset applied all 16 migrations through `00000000000015_batch_15.sql`. The complete pgTAP suite passed 13 files / 438 assertions / 0 failures. Deno 2.9.5 passed all 3 Edge test files / 13 tests / 0 failures, covering invitation resolve, RSVP, and Wedding deletion. Flutter passed 31 tests / 0 failures; analyzer completed with 0 errors, 0 warnings, and 208 info diagnostics. Guest Web passed 2 suites / 9 tests / 0 failures in 24.12 seconds, lint passed, and the production build passed in 1.24 seconds with a 202.68 kB JavaScript bundle (64.16 kB gzip).

Final real local Storage HTTP verification used disposable synthetic WebP fixtures. Service-role uploads, listing, nested-prefix listing, and bounded deletion returned HTTP 200. A fresh root and nested list were empty after cleanup, an empty-prefix retry remained empty, and an object under another Wedding prefix remained present. An ACTIVE OWNER direct DELETE returned HTTP 200 but the object remained readable; therefore HTTP success is not treated as authorization evidence, and object persistence plus the continued absence of an organizer DELETE policy proves no effective organizer delete authority. Server cleanup succeeds independently of organizer DELETE permission. Fixtures and the temporary Auth user were removed after verification.

Final security review passed: archive is an authenticated `auth.uid()`-bound OWNER operation; delete requires Edge bearer verification and a server-derived Auth user ID; `edge_api` bridges are service-role-only; DB authorization independently requires ACTIVE OWNER membership; no custom GUC impersonation is used; bucket and Wedding prefix are server-derived; no generic cleanup or arbitrary database-delete surface exists; service credentials and tokens are neither logged nor persisted; errors are bounded; `auth.users`, unrelated Weddings, and non-Wedding-scoped Class-D limiter rows are preserved. ARCHIVED and DELETING lifecycle gates keep guest resolve, RSVP, new media signing, ordinary mutation, and organizer Storage mutation fail-closed as designed.

Controlled amendment `M7-AUTH-BRIDGE-001` remains the only M7 architecture amendment. No open M7 implementation conflict remains. Non-blocking debt: real provider pagination was not forced beyond the small local fixture, while deterministic Edge coverage proves the 100-entry page and 100-path delete-batch branches; the existing Flutter analyzer baseline contains 208 info-level diagnostics. The local Storage DELETE endpoint's HTTP 200/no-op behavior requires post-condition verification, which the implementation and evidence explicitly use.
